// Legacy axios configuration file.
// Kept for backward compatibility – it now re-exports the new central config
// from `src/config/axios.ts`. Prefer importing from `@/config/axios` in new code.

export * from "../config/axios";
